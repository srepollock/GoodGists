<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Platform.AppCompat.Light">
    </style>

    <style name="MaterialTheme" parent="Theme.AppCompat" />

    <style name="Theme.MaterialTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar" />

</resources>
